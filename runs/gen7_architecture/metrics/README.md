Per-seed metric tables, indexed here and stored beside the suite that produced them.
`leaderboard_all.csv` at the top level is the cross-suite view; `all_scores_by_seed.csv`
is every row of every suite concatenated.
