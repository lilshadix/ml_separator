# Accepted-union provenance

These immutable input snapshots produced `accepted_geometries.csv` and `excluded_geometries.csv`. Historical host paths are provenance only; the packaged `geometries/` files are the canonical transferable assets.
