# D07 -- Were the four frozen claims confirmed on the withheld seeds, and was Pr/Nd selectivity reconstructed with the pair hidden? (pre-registration section 15, section 9 S2, section 3.4; brief section 34 Q3)

*Written 2026-09-24 by the figures / D07 task at git HEAD `bdc62e2aa3c9c9088c731605d292afa75d602a3b`, from files only; paths are relative to `generations/gen19_chem_transfer/`. **The confirmation run (the four frozen claims on the 5 withheld seeds) and the single V6 Pr/Nd run were NOT RUN.** Nothing in this file was computed on a withheld seed, on the confirmation half of any design, or on V6: no such number exists anywhere in the repository. Every discovery number below is the selection half, discovery seed 104729 -- "discovery, optimistically biased" (pre-registration section 15; POST-HOC addendum 1 item 3) -- and carries R19 item 4 NOT_EVALUATED.*

## Question

Pre-registration section 15 registers one run, once, on the 5 withheld seeds: it scores the frozen claims of `decisions/CONFIRMATION_PLAN.md` on the **confirmation half** of each design with R19 item 4 as registered (delta > 0 in 5 of 5 seeds; a claim is confirmed only then), and runs **V6 once** with S2(a)-(c) (POST-HOC addendum 5 item 1: the core run, without the section 11 V6 actinide deltas and without a section 8 power check). Two questions follow. (1) Were C1, C2, C3 and C4 confirmed? (2) Was Pr/Nd selectivity reconstructed when the Pr(III) and Nd(III) cells of the 13 V6 systems were hidden together (brief section 34 Q3; section 9 S2)?

## Evidence

| file | contents |
|---|---|
| `decisions/CONFIRMATION_PLAN.md` | the four frozen claims (section 2), the core scope under addendum 5 (section 0), the order of work (section 5), the compute estimate (section 6); status line updated 2026-09-24: NOT RUN |
| `manifests/g19_run_confirmation_dry_run.json` | the captured stdout of `scripts/g19_run_confirmation.py --dry-run` (`dry_run_exit_code` 0; `dry_run.note`: no seed was read, nothing was written) and the refusal of `--check-only` without a seed store (`check_only_exit_code` 1, `check_only_stderr`: "refused: the confirmation run needs --seed-store PATH; the 5 withheld seeds enter only through it (section 15)") |
| `scripts/g19_run_confirmation.py` -> `RUN_STAGES`, `refusal()` | the 16 stages of the single run and which this tree implements: 14 implemented, **2 NOT IMPLEMENTED** -- `v6_frozen_configurations` (text `FROZEN_CONFIGURATIONS`) and `c4_act_permuted_training_transform` (text `ACT_PERMUTED_LEG`); the runner refuses at the first unimplemented stage BEFORE a manifest, a fold file or the once-only lock is written (`main()`) |
| `tests/test_confirmation.py::test_the_v6_dispatch_is_the_v5_inner_design_and_the_refusal_machinery_still_works`, `::test_e7_the_v6_run_is_not_at_frozen_configurations_and_c4s_control_is_not_permuted` | assert that exactly those two stages are unimplemented, that the refusal names both, and that "the single registered run (section 15) has NOT been spent" |
| `manifests/digest_registry.json -> stages.confirmation` | `code_digest` `dddaa0d342694ea3943c903db050addc54ccfb4a577f8b64287014090167b530`, `registered_utc` 2026-09-23T13:03:08+00:00, `git_head` `5f8ccd8812d4138f9753a7e8e7edc1a78c3bdfdc`, `below_footer_sha256` `da38be2522292700d77f5a7df4bc6de830155393be7bedfd272376288c9caadc`, `addenda_count` 8 |
| `manifests/digest_registry.json -> code_changes` | 15 entries touching `scripts/g19_run_confirmation.py` or `gen19ct/evaluation/confirmation.py`, `logged_utc` 2026-09-23T03:33:12+00:00 to 2026-09-23T13:03:05+00:00: the runner's creation, the run body, and the verification rounds labelled "TASK X" (06:23-06:34Z), "Task X" (09:06-10:00Z) and "TASK X round 4" (12:45Z, 13:03Z) |
| `manifests/confirmation_seeds_sha256.txt` | the section 15 commitment `65e8ae8ceb8e92947a1da27f42e2689e74b04cc7b820d2b6df26889ceabf5f82` (`manifests/g19_commit_seeds.json -> arguments.n` = 5, `git_head` `40f6a75047275de8f690d61391935df4f916e226`); the seeds themselves live outside the repository and were never read by any runner |
| `evaluation/confirmation/` | **absent**: neither the lock marker `evaluation/confirmation/decisions/run_started.json` (`confirmation.started_path`) nor `evaluation/confirmation/decisions/confirmation.json` (`confirmation.decisions_path`) exists; `decisions/CONFIRMATION.md` (`confirmation.report_path`) does not exist |
| `evaluation/discovery/contrasts_registered.csv`, `evaluation/power/power_checks.json` | C1-C3's discovery rows (`key`, `cluster_unit` = system) and their section 8 power verdicts (`checks[0]`, `checks[2]`, `checks[3]`) |
| `evaluation/h3/h3_contrasts.csv`, `evaluation/h3/h3_r19_items.csv`, `evaluation/h3/h3_summary.json -> power_debt` | C4's discovery row (`key` `B6:WITH vs B6:ACT_PERMUTED@V2`, `cluster_unit` metal_state), its R19 items, and its power-check status |
| `evaluation/discovery/decisions/decisions.json -> S1_components` | the S1 components as they stand after discovery (all UNDECIDED or without a verdict; the S1(c) selection-half counterweight) |

**Why it was not run.** The once-only runner went through four verification rounds on 2026-09-23 (the `code_changes` entries above), each of which found blocking defects by executing the run body on a synthetic corpus; the last round (`code_changes` 12:45:06Z and 13:03:05Z; commit `bdc62e2`) found that a missing M1 record would abort the whole run rather than one fold, that a spent run was resumable, that C4's two legs shared one record path, and that the M1 pairing digest did not pin the values M2 consumes. Two stages of `RUN_STAGES` remain NOT IMPLEMENTED (`scripts/g19_run_confirmation.py`):

- `v6_frozen_configurations` -- section 3.4 registers the single V6 run "with the frozen configurations"; the tree hands every V6 job the tuning runners, so (measured on a synthetic V6 fold) a V6 M1 fold re-tuned 9 configurations over 27 inner fits and chose its own `selected_config`. Which configuration a V6 fold inherits (the deployed one of addendum 3 item 2, the per-seed V5-primary selection, or the seed-104729 one) is a REGISTERED CHOICE that no addendum makes; the frozen-refit machinery exists (`h3.frozen_runner`).
- `c4_act_permuted_training_transform` -- the confirmation loop never calls `h3.transformed_frame`, `discovery.h3_training_rows` or `h3.frozen_runner` (call counters 0 over a complete `run_fold`), so C4's ACT_PERMUTED leg would be fitted on the RECORDED log D and re-tuned; three registered pieces are missing: the section 11 permutation of training log D within (system, publication group), the plan's frozen refit off the WITH record of the same fold (`h3.FrozenB6` through the sibling-record locator), and addendum 5 item 2's guard rule for a value-permuted arm.

By the orchestrator's own rule no further round was run: the track was stopped on 2026-09-24 to deliver the report from discovery, the ladder, the power check and H3 (commit `bdc62e2` message; `decisions/CONFIRMATION_PLAN.md` status).

## Metrics

**The four frozen claims, discovery values (selection half, discovery seed 104729, optimistically biased; R19 item 4 NOT_EVALUATED, full R19 UNDECIDED).** Delta = MAE(comparator) - MAE(candidate), unit-macro MAE of log D.

| claim | contrast | design | cluster unit (clusters / units) | delta (point) | margin | percentile 95 % | BCa 95 % | p (two-sided) | mde_80 | LOCO min | items 1, 2, 3, 5, 6 | item 4 | full R19 | section 8 power | source |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| C1 | M2 vs B3i | V5-primary | system (18 / 105) | **+0.262976** | 0.105689 (delta5) | [0.091396, 0.431484] | [0.118816, 0.469207] | 0.0040 | 0.242920 | +0.193542 | PASS (`verdict_freezing_screen` PASS) | NOT_EVALUATED | UNDECIDED | POWERED_NOT_A_NULL, kappa_min 0.1 | `contrasts_registered.csv` key `M2 vs B3i@V5`; `power_checks.json -> checks[0]` |
| C2 | M2 vs B0 | V5-primary | system (18 / 105) | **+1.022150** | 0.05 | [0.667279, 1.431860] | [0.716473, 1.559030] | 0.0000 | 0.552331 | +0.854188 | PASS | NOT_EVALUATED | UNDECIDED | POWERED_NOT_A_NULL, kappa_min 0.1 | key `M2 vs B0@V5`; `checks[2]` |
| C3 | M2 vs B6r0 | V5-primary | system (18 / 105) | **+0.455132** | 0.05 | [0.259001, 0.618706] | [0.286876, 0.642987] | 0.0002 | 0.253770 | +0.388074 | PASS | NOT_EVALUATED | UNDECIDED | POWERED_NOT_A_NULL, kappa_min 0.1 | key `M2 vs B6r0@V5`; `checks[3]` |
| C4 | B6:WITH vs B6:ACT_PERMUTED | V2 (`V2__element_exact`) | metal_state (7 / 7) | **+0.0815187** | 0.05 | [0.0355927, 0.136451] | [0.0411925, 0.147557] | 0.0000 | 0.0727359 | +0.0578916 | PASS (`h3_r19_items.csv`: items 1, 2, 3, 5, 6 PASS) | NOT_EVALUATED | UNDECIDED | UNDECIDED (no registered power check): `h3_summary.json -> power_debt.contrasts[key B6:WITH vs B6:ACT_PERMUTED@V2].check_run` false, `n_refits` 96, `cost_hours_serial` 0.24 | `h3_contrasts.csv` key `B6:WITH vs B6:ACT_PERMUTED@V2`, `cluster_unit` metal_state |

- C1-C3's item 6 is the reduced sensitivity set (addendum 1): `sensitivity_set` "reduced sensitivity set (addendum 1)", `sensitivities_not_run` "V5-P, V5-cell-only, loose_setting, parent_structure_hiding, sr_iii_dropped_training" (`contrasts_registered.csv`, the same three rows).
- C4's `mde_80` 0.0727359 exceeds its 0.05 margin: a FAIL of item 1 would have been uninformative; item 1 PASSED (`h3_r19_items.csv`). The H3 power debt as inventoried now: `h3_summary.json -> power_debt.n_owed` 24, `n_owed_and_run` 0, `n_refits_owed_and_not_run` 7,520, `cost_hours_serial_owed_and_not_run` 477.434, `label_when_not_run` "UNDECIDED (no registered power check)". (Addendum 5 item 4 quoted 20 contrasts, 6,272 refits, about 436 h; the inventory grew when the two `ACT_PERMUTED@V1` legs were completed under addendum 5 item 2, `CONFIRMATION_PLAN.md` section 0.)
- S1 components after discovery (`decisions.json -> S1_components`): `S1a_M2_vs_B3i` UNDECIDED, `S1b_M2_vs_B0` UNDECIDED, `S1b_M2_vs_B6r0` UNDECIDED, `S1c_selection.reported_verdict` UNDECIDED with the selection-half counterweight met -- `S1c_selection.min_delta` -0.016846876701018858 (`min_delta_yardstick` HEAVIER, `n_pairs` 9,802) is within the registered -0.02 (section 9 S1(c), addendum 2) -- and `S1d_calibration`, `S1e_support_distance` without a verdict in that block; `label` "discovery, optimistically biased (selection half; S1 is decided on the confirmation half with the withheld seeds, section 15)".

**Confirmation values.** For every claim, for S1(c) at confirmation, S1(d), S1(e), S2(a), S2(b), S2(c) and S2(d): **not computed (input missing: `evaluation/confirmation/decisions/confirmation.json`)**. No arm has ever been fitted or scored on a V6 fold or on a confirmation-half fold; no withheld seed has been read.

**Planned cost of the core run** (`manifests/g19_run_confirmation_dry_run.json -> dry_run`; measured per-fold unit costs of the discovery and H3 records, `dry_run.basis`):

| quantity | value | key |
|---|---|---|
| serial compute | **110.24 h** | `dry_run.serial_hours` = 110.2423388888889 |
| wall clock at 2 workers (measured parallel efficiency 1.96x) | **56.25 h** | `dry_run.wall_hours_2_workers` = 56.24609126984127 |
| jobs / folds | 85 / 3,418 | `dry_run.n_jobs`, `dry_run.n_folds` |
| claims C1-C3 core (M1 then M2 on the confirmation V5-primary folds) | 37.065 h | `dry_run.by_purpose_serial_hours["claims C1-C3 (core)"]` |
| R19 item 6 refits (seed 104729 only, addendum 1 item 4 / addendum 5 item 1) | 9.113 h | `["R19 item 6 refits (discovery seed 104729 only)"]` |
| R19 item 6 refit comparators (closed form) | 0.740 h | `["R19 item 6 refit comparators (closed form, seed 104729)"]` |
| claim comparators B3i, B0 (closed form) | 1.542 h | `["claim comparators (closed form)"]` |
| claim C3 comparator B6r0 | 1.480 h | `["claim C3 comparator B6r0"]` |
| claim C4 (H3 control contrast) | 0.177 h | `["claim C4 (H3 control contrast)"]` |
| S1(c) direction and logSF on V5-PAIR (M1 + M2) | 51.369 h | `["S1(c) direction and logSF (V5-PAIR)"]` |
| S1(c) yardstick refits (closed form) | 0.084 h | `["S1(c) yardstick refits (closed form)"]` |
| the single V6 run (S2), learned arms at the frozen configurations | 8.488 h | `["the single V6 run (S2)"]` |
| the single V6 run, yardsticks | 0.184 h | `["the single V6 run (S2) -- yardsticks"]` |
| fold files built inside the run (addendum 6 item 2) | 25 = 5 stems x 5 seed indices: `V5__primary__batched_max4` 27 folds, `V5__strict__batched_max4` 8, `V5__hno3_only__batched_max4` 27, `V5PAIR__primary__batched` 38, `V6__prnd__exact` 13, per withheld seed | `dry_run.fold_files_to_build[*].{stem, seed_index, n_folds}` |
| not run inside the core run | `v6_actinide_deltas` NOT_RUN, `power_check` NOT_RUN | `dry_run.not_run` |

`dry_run.basis` adds two cautions that stand: the band is +-30 % (discovery overran its 60 h budget by 28 %), and the V6 lines assume the section 3.4 frozen configurations -- run as the tree stands (re-tuning) the V6 block would be about 30 h and the plan about 132 h, which is why the runner refuses at `v6_frozen_configurations`. Decomposition of the 110.24 h from `dry_run.by_purpose_serial_hours` alone: the closed-form comparator and yardstick lines total 2.55 h (`["R19 item 6 refit comparators (closed form, seed 104729)"]` 0.7404 + `["claim comparators (closed form)"]` 1.5417 + `["S1(c) yardstick refits (closed form)"]` 0.0844 + `["the single V6 run (S2) -- yardsticks"]` 0.1842), C3's B6r0 line is 1.48 h (`["claim C3 comparator B6r0"]`), and the remaining six priced lines total 106.21 h (`dry_run.serial_hours` 110.2423 minus those two); the plan's table before this manifest existed carried a bare `--dry-run` total with no manifest behind it, and `CONFIRMATION_PLAN.md` section 0 now carries 110.24 / 56.25 with this file as its source.

## Verdict (null / supported / ambiguous)

**ambiguous / not run.**

- The confirmation run: **NOT_RUN**. The single V6 run: **NOT_RUN**.
- C1, C2, C3, C4: **UNDECIDED** (R19 item 4 NOT_EVALUATED; discovery values only). Not one of them is a null: C1-C3 are POWERED_NOT_A_NULL in discovery and C4 has no registered power check.
- S2(a), S2(b), S2(c): **NOT_RUN**. S2(d): NOT_RUN (Phase H has not run; `decisions/D06_process_integration.md`). Brief section 34 Q3 -- can Pr/Nd selectivity be reconstructed when direct pair data are hidden? -- is **NOT ANSWERED**.
- S1 (section 9): **UNDECIDED** -- S1(a) and S1(b) await item 4, S1(c) awaits the confirmation-half contrast (the selection-half counterweight holds), S1(d) and S1(e) are evaluated at confirmation.
- The absence of a confirmation result is an absence of evidence about the frozen claims, not evidence against them; no INFORMATIVE_NULL power record exists for any of them, so no sentence about them may read as a null.

## Decision

1. The run is recorded NOT_RUN. The withheld seeds remain committed (`manifests/confirmation_seeds_sha256.txt`, `65e8ae8c...5f82`) and unused; the once-only lock is unspent; nothing has run on V6 or on the confirmation half of any design. Whatever a future run returns is the result (section 15: no sixth claim is added and nothing is re-run).
2. The report states the omission on its first screen. Figure 12 (Pr/Nd hidden-cell reconstruction) and the V6 panel of figure 13 are skipped with the reason "confirmation run not run" (`evaluation/figures/figures_index.json`); they can only be drawn from `evaluation/confirmation/v6_rows.csv` and `v6_pairs.csv`.
3. The deployed predictor stays **M0** (alias B5 in every record and table; addendum 3 item 2), and for lanthanide prediction the **WITHOUT-actinide** fit (section 11 consequence, addendum 5 item 3; `decisions/D03_actinide_transfer.md`). With S1 UNDECIDED the process layer (section 14) may run only in exploratory mode with every output labelled transfer-unsupported, and gen18's B1 nearest-condition lookup remains the process chain's default D source (section 10).

## Next action (what a future session must do to run it)

1. **Implement the two `RUN_STAGES` blockers** in `scripts/g19_run_confirmation.py` and flip their flags to `True` only with the tests that assert them (`tests/test_confirmation.py::test_the_v6_dispatch_is_the_v5_inner_design_and_the_refusal_machinery_still_works`, `::test_e7_the_v6_run_is_not_at_frozen_configurations_and_c4s_control_is_not_permuted`):
   - `v6_frozen_configurations`: first an orchestrator decision, recorded as a POST-HOC addendum below the footer, naming which configuration a V6 fold inherits (the deployed configuration of addendum 3 item 2; the per-seed V5-primary selection; or the seed-104729 selection) -- then the V6 jobs go through the frozen-refit machinery (`h3.frozen_runner`, `FrozenNeural` / `FrozenB6`) with no re-tuning, and the cost line of `CONFIRMATION_PLAN.md` section 6 is re-checked with `--dry-run`.
   - `c4_act_permuted_training_transform`: (a) the section 11 permutation of training log D within (system, publication group) on the fold's TRAINING rows only (`h3.transformed_frame`); (b) the plan's rule for the leg, a frozen refit at the WITH run's selected hyperparameters of the same fold (`h3.FrozenB6`, through the sibling-record locator the runner already has for M2's M1 record); (c) addendum 5 item 2's guard rule (the near-duplicate VALUE comparison reads the corpus's recorded log D for a value-permuted arm).
2. **Re-register the `confirmation` stage from the tree before launch.** Gate (b) compares the live code digest with `stages.confirmation.code_digest`; the 2026-09-24 verification fix in `scripts/g19_run_discovery.py` (`code_changes`; it lets every verified reader recompute a record set's fold digests under the registry entry it was written under, addendum 7 item 3) is inside the confirmation stage's code closure, so the live digest already differs from the registered `dddaa0d3...67b530`. After the code is final: `PYTHONIOENCODING=utf-8 PYTHONPATH=generations/gen19_chem_transfer .venv/Scripts/python.exe -m gen19ct.evaluation.registry register --stage confirmation --force --note "<why>"`; after any new addendum, `reregister-all` (addendum 7 item 3).
3. **Verify without spending anything**: `... scripts/g19_run_confirmation.py --dry-run` (no seed store needed; fits nothing, writes nothing), then `... scripts/g19_run_confirmation.py --check-only --seed-store <PATH>` (the five gates; writes nothing).
4. **Launch, once** (section 15): `PYTHONIOENCODING=utf-8 PYTHONPATH=generations/gen19_chem_transfer .venv/Scripts/python.exe generations/gen19_chem_transfer/scripts/g19_run_confirmation.py --seed-store <PATH> --workers 2`, where `<PATH>` is the seed store **outside the repository, known to the user** -- the only way the 5 withheld seeds enter the code; the runner verifies it against `manifests/confirmation_seeds_sha256.txt`, scrubs every seed from every artefact and reveals them only in `decisions/CONFIRMATION.md` after the run (addendum 6 item 4). Expect 110.24 h serial / 56.25 h wall at 2 workers, +-30 %. `--workers` is capped at 2 (the measured limit of the machine).
5. **Resume only to complete unfitted folds** (`--resume`; addendum 8 item 2): the recorded code digest must be the live one and the claim list may not grow; once `evaluation/confirmation/decisions/confirmation.json` exists the run is spent and the runner refuses.
6. **After the run**: rerun `scripts/g19_make_figures.py` (figure 12 and the V6 panel of figure 13 read `evaluation/confirmation/v6_rows.csv`, `v6_pairs.csv`) and `scripts/g19_build_report.py`; Phase H (section 14) as a registered run only if S1 passes, otherwise exploratory and labelled transfer-unsupported.
