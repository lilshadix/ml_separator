# D06 -- Does the Gen19 predictor change the TODGA Pr/Nd process recommendation, and does it hold under the predictor's uncertainty? (H7, sections 14, 9 S2(d), 10 F5)

*Written by task G2 at git HEAD `bdc62e2aa3c9c9088c731605d292afa75d602a3b` from the files cited below (paths relative to `generations/gen19_chem_transfer/`). Format follows brief section 29. This file replaces the runner-generated stub of 2026-09-19 because the runner could not run; `scripts/g19_run_process.py --decision-only` would regenerate a generic "Phase H has not run" version without the reasons recorded here. No code was changed, no learned arm was fitted, no confirmation-half row, V6 target row or withheld seed was read (`evaluation/process/attempts/g2_exploratory_attempt_2026-09-23.json` -> `no_code_changed`, `no_learned_arm_fitted`, `confirmation_half_read`, `v6_target_rows_read`, `withheld_seeds_read`).*

**State on the first screen.**

- **The confirmation run (the 4 frozen claims on the 5 withheld seeds) and the single V6 Pr/Nd run were NOT RUN.** `evaluation/confirmation/decisions/confirmation.json` does not exist (attempt file -> `blocker_2_inputs.confirmation_decision.exists` = false). S1 is therefore UNDECIDED and S2 is not answered, so section 14's gate admits the process layer in exploratory mode only, with every output labelled **transfer-unsupported** (addendum 3 item 3; `decisions/CONFIRMATION_PLAN.md` item 7).
- **The exploratory process run was attempted and REFUSED before its first Monte Carlo draw.** `scripts/g19_run_process.py --exploratory` exited with code 1 after 6.812 s of wall clock (attempt file -> `attempts[1].exit_code`, `attempts[1].wall_clock_seconds`; start `2026-09-23T13:38:33Z`, end `2026-09-23T13:38:40Z`). Zero draws ran; no process table, no `winners.csv`, no F14 / F15 figure and no `manifests/g19_run_process.json` was written (`attempts[1].outcome`).
- **Two independent blockers**, both recorded below: (1) at HEAD `bdc62e2` the runner's gate item 2(d) raises `StaleRecordError` on every discovery record because the discovery stage was re-registered under the 8-addenda digest while its records carry the addendum-1 digest; (2) the prediction table the runner consumes has no producer in the repository. The 2 h wall-clock cap of the task was never reached and no design-count reduction was made.
- **A concurrent, uncommitted edit not made by this task** appeared in the working copy of `scripts/g19_run_discovery.py` at `2026-09-23T13:45:04Z`, after both attempts (attempt file -> `concurrent_tree_modification`: HEAD sha256 `92bf655d...`, working-copy sha256 `2933bb8a...`, 61 insertions / 4 deletions). It addresses blocker 1 (update 2026-09-24: since logged as `manifests/digest_registry.json` -> `code_changes[40]`, 14:23:07Z; blocker 1 closed, blocker 2 operative -- Decision, bullet 4). Re-run against it once, read-only: `--check-only` passed item 2(d) and refused at item 4 as designed (no `confirmation.json`; 24.028 s), and `--exploratory` passed the gate and refused at blocker 2 -- "the Gen19 prediction table ... is missing" (24.324 s, exit code 1; `attempts[2]`, `attempts[3]`). **Blocker 2 is therefore the operative one**; the exploratory run still cannot start.
- Deployed predictor: **M0**, whose records and lookups use the alias **B5** (`evaluation/h3/h3_summary.json` -> `deployed.arm`, `deployed.arm_alias`). Under section 11 as read by addendum 5 item 3, the configuration deployed for lanthanide prediction is the **WITHOUT-actinide fit** of M0. **No fit of M0 that can serve the prediction request exists, WITHOUT or WITH** (see Metrics, "which fit").

## Question

Fed by the deployed Gen19 predictor's records for Pr and Nd in the TODGA / HNO3 / aliphatic system `sys_5cb78e5000d40860`, does gen18's cascade find a recipe that meets the registered purity x recovery grid (purity {0.95, 0.97, 0.99} x recovery {0.80, 0.85, 0.90}; `preregistration.md` section 14 "Case") with P(both targets and feasible) that survives the D-draw uncertainty (S2(d) >= 0.80), without any UNSUPPORTED prediction (F5(i)) and without the optimum depending on barred predictions (F5(ii))?

## Evidence

| file | status | contents |
|---|---|---|
| `evaluation/process/attempts/g2_exploratory_attempt_2026-09-23.json` | present (this task) | the two attempts, the exception, the digest reproduction, the sampled record sets, the registry entries, the input inventory, the existing fits of M0 |
| `evaluation/process/inputs/todga_prnd_prediction_request.csv` | present (2026-09-19) | the 450 conditions the deployment step must predict; every `mean_logD` and `arm` cell empty |
| `evaluation/process/inputs/todga_prnd_predictions.csv` | **absent** | the deployed predictor's records on the grid -- the runner's required input |
| `evaluation/process/inputs/prnd_residual_correlation.json` | **absent** | the section 14 Pr / Nd residual correlation (exploratory fallback: rho = 0, flagged) |
| `evaluation/confirmation/decisions/confirmation.json` | **absent** | the S1 / S2 / V6.run decision of the confirmation run |
| `evaluation/process/summary.json`, `winners.csv`, `stability.json`, `f5.json`, `operating_points.csv`, `process_table.csv` | **absent** | the runner's outputs |
| `tables/process_*.csv`, `figures/F14_process_pareto_uncertainty.png`, `figures/F15_probability_of_specification_map.png` | **absent** | brief section 25 items 14 and 15 |
| `evaluation/discovery/decisions/stop_rule.json` | present | `stop` = false; `M2_vs_B3i.verdict` = PASS; `B6_vs_B3i.verdict` = FAIL |
| `evaluation/h3/h3_summary.json`, `evaluation/h3/h3_f4.json` | present | the deployed configuration and F4 |
| `manifests/digest_registry.json` | present | stage entries and superseded entries |

## Metrics

### Headline P(both targets) rows -- label transfer-unsupported

| cell (P_min, R_min) | winner | support rank | P(both & feasible) | P(both) | P(feasible) | purity p50 [p5, p95] | recovery p50 [p5, p95] | stages (ext+scr+str) | O/A | consumption | S2(d) kept | F5(ii) |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| (0.95, 0.80) | not computed (input missing: `evaluation/process/winners.csv`) | | | | | | | | | | | |
| (0.95, 0.85) | not computed (input missing: `evaluation/process/winners.csv`) | | | | | | | | | | | |
| (0.95, 0.90) | not computed (input missing: `evaluation/process/winners.csv`) | | | | | | | | | | | |
| (0.97, 0.80) | not computed (input missing: `evaluation/process/winners.csv`) | | | | | | | | | | | |
| (0.97, 0.85) | not computed (input missing: `evaluation/process/winners.csv`) | | | | | | | | | | | |
| (0.97, 0.90) | not computed (input missing: `evaluation/process/winners.csv`) | | | | | | | | | | | |
| (0.99, 0.80) | not computed (input missing: `evaluation/process/winners.csv`) | | | | | | | | | | | |
| (0.99, 0.85) | not computed (input missing: `evaluation/process/winners.csv`) | | | | | | | | | | | |
| (0.99, 0.90) | not computed (input missing: `evaluation/process/winners.csv`) | | | | | | | | | | | |

Had the run produced them, every row would carry the label `transfer-unsupported` and none could be a headline recommendation (section 14 "Gate"; `scripts/g19_run_process.py` `transfer_label`).

### What ran, and how it ended

- `scripts/g19_run_process.py --check-only`: `StaleRecordError` raised inside `refuse_unless_ready -> discovery_complete -> h3.discovery_complete -> g19_run_discovery.verified_predictions -> discovery.read_discovery_record_set`; the gate verdict was never printed; 6.684 s (attempt file -> `attempts[0]`).
- `scripts/g19_run_process.py --exploratory`: the same exception at the same place; 6.812 s; exit code 1 (attempt file -> `attempts[1]`). Verbatim (`attempts[1].exception`): `gen19ct.evaluation.discovery.StaleRecordError: evaluation/discovery/B6/V5__primary_exact/s104729/Am(III)__1bf1228273.parquet: record digest d107751e18eb... differs from the current a3432712a289... (code, fold file or plan state changed; rerun the job)`.
- Against the concurrently modified working copy (not this task's edit; `concurrent_tree_modification`): `--check-only` exit code 1 after 24.028 s, item 2(d) passed, refused at item 4 in registered mode because `confirmation.json` is missing (`attempts[2]`); `--exploratory` exit code 1 after 24.324 s, gate passed, refused at blocker 2 with the verbatim message "refused: the Gen19 prediction table evaluation/process/inputs/todga_prnd_predictions.csv is missing. The conditions to predict were written to evaluation/process/inputs/todga_prnd_prediction_request.csv ..." (`attempts[3].message`). That run rewrote the request file: 450 rows unchanged, 17 -> 23 columns, the six empty `member_logD_0..4` / `conformal_q95` columns added, every condition value identical, every fill column empty (`prediction_request_rewritten`: `sha256_before` `a973cdd4...`, `sha256_after` `d1034570...`, `values_identical_on_shared_columns` = true, `all_fill_columns_empty` = true). The file is the runner's own current-schema output and was left in place, not git-added.
- Monte Carlo: 0 draws of the registered 64 (section 14) over 0 of the default 1,000 LHS designs (`scripts/g19_run_process.py` `REGISTRATION_READINGS['design_count']`; `gen19ct/process/monte_carlo.py` `N_DESIGNS_DEFAULT`) in every attempt. The 2 h cap was not consulted; the design count was not reduced.

### Blocker 1 -- gate item 2(d) cannot pass at HEAD `bdc62e2` (attempt file -> `blocker_1_gate`)

Status: raises at HEAD `bdc62e2` (`attempts[0]`, `attempts[1]`); passes under the uncommitted, not-this-task working copy of `scripts/g19_run_discovery.py` (`attempts[2]`, `attempts[3]`; `blocker_1_gate.status_after_concurrent_modification`).

- The record the gate stops on: `evaluation/discovery/B6/V5__primary_exact/s104729/Am(III)__1bf1228273.json`, job `fit:B6:V5__primary_exact:s104729`, registry stage `discovery` (`first_stale_record.record_json`, `job_key`, `registry_stage_of_job`).
- Its `code_digest` equals the registry's discovery code digest and its `fold_hash` equals the expected one (`first_stale_record.code_digest_equal` = true, `fold_hash_equal` = true). Only the composite record digest differs: stored `d107751e18eb4b7f23f48ff8e479fe713674dcb826d58b083d0bdc69b3c1e01b`, expected under the current registry `a3432712a28927f3dc6655e780fc8d13a925c4601820c68978074dc5cb324eda` (`record_digest`, `expected_digest_under_current_registry`).
- Cause, reproduced: `fold_digest` (`scripts/g19_run_discovery.py:1113`) folds `REG.below_footer_sha256(REG.job_stage(job))` -- the CURRENT discovery entry's below-footer digest, `da38be2522292700d77f5a7df4bc6de830155393be7bedfd272376288c9caadc` (8 addenda) -- into the expected digest, while the record was written under the addendum-1 digest `05f36fc0354adb75ae41655977115d827ffce1227b2bdbfeeba28a975f0e8e54` (`record_prereg_addenda_sha256`, `record_prereg_n_addenda` = 1). Recomputing the expected digest with the record's own addenda digest gives `d107751e18eb...`, identical to the stored one (`expected_digest_with_record_addenda_sha256`, `digest_equal_with_record_addenda_sha256` = true).
- Scope: 6 of 6 sampled record sets raise `StaleRecordError` under the current registry -- B5, M2 and FLAT_CAT on `V5__primary_batched_max4`, B5 and B6 on `V1__copy_exact`, B6 on `V5__primary_exact`, all seed 104729 (`sampled_record_sets[*].status`). By construction every discovery record carries the addendum-1 digest, so the check fails on the first record of every set.
- Registry timeline (`manifests/digest_registry.json` -> `stages.discovery`, `superseded[stage = discovery]`; attempt file -> `registry_stage_entries`, `superseded_discovery_entries`): the discovery entry with `addenda_count` 1 was registered `2026-09-22T01:53:20+00:00` and superseded `2026-09-23T07:03:30+00:00` (addendum 7, git head `388f3a0`); the entry with `addenda_count` 7 was superseded `2026-09-23T10:27:05+00:00`; the current entry has `addenda_count` 8, `below_footer_sha256` `da38be25...`, `git_head` `5f8ccd8`, `code_digest` `895a6ab9...` unchanged throughout. The registry's own rule says a record verifies against the entry it was written under (`rule`; `registry.verify_record`, `gen19ct/evaluation/registry.py:272`, implements that superseded-entry fallback), but the discovery-completeness path does not call it.
- The same gate function is shared by `scripts/g19_run_h3.py:158`, `scripts/g19_make_figures.py:117` and `scripts/g19_build_report.py:93` (`blocker_1_gate.shared_by`). Whether those runners are affected the same way was not tested by this task.

### Blocker 2 -- the prediction table has no producer (attempt file -> `blocker_2_inputs`)

- `evaluation/process/inputs/todga_prnd_predictions.csv`: absent (`prediction_table.exists` = false). The runner refuses without it (module docstring "Inputs"; `run_case`).
- The request the runner wrote on 2026-09-19 (`prediction_request`): 450 rows, metals Nd and Pr, system `sys_5cb78e5000d40860`, acid HNO3, anion nitrate, 25 log-acid x 9 log-ligand grid, acid 0.00933254 to 5.0 M, TODGA 0.00023126 to 0.3 M, 0 of 450 `mean_logD` filled, 0 `arm` filled, no `member_logD_k` / `conformal_q95` columns (`n_mean_logD_filled`, `n_arm_filled`, `member_columns_present`).
- No deployment prediction step exists: the only code touching the table is the runner (writes the request) and `gen19ct/process/gen18_adapter.py` (the schema); `models/` is empty (`deployment_prediction_step.exists` = false, `searched`, `models_dir_entries` = []).
- `evaluation/process/inputs/prnd_residual_correlation.json`: absent (`rho_file.exists` = false). Not blocking on its own in exploratory mode: the runner would draw Pr and Nd independently with rho = 0, flagged, which section 14 says inflates logSF uncertainty (`rho_file.exploratory_consequence`).

### Which fit the prediction request would use, and why none is available (attempt file -> `deployed_predictor_fits`)

- Deployed configuration: M0 = B5, "the ladder retains its base step M0 ..., M1 and M2 are not kept, and no step M3-M7 is kept" (`evaluation/h3/h3_summary.json` -> `deployed.arm`, `deployed.arm_alias`, `deployed.basis`; `evaluation/ladder/decisions/ladder.json` -> `steps.M3..M7.status` = not_run).
- Section 11 (addendum 5 item 3): actinide rows enter the deployed Ln configuration only on a *helps* verdict; the verdict is UNDECIDED, so the lanthanide deployment is the **WITHOUT-actinide fit of M0**. The prediction request for Pr and Nd would therefore be served by a WITHOUT fit, not by the WITH fit the H3 evaluation calls "deployed" (`evaluation/h3/h3_f4.json` -> `deployed_arm` = B5, `deployed_trained_with_actinide_rows` = true). F4 is stated plainly beside it: `failure` = true (BCa on V2: `per_design.V2.point` = 0.026415684977688314, `per_design.V2.bca_95` = [0.003148734214400464, 0.078274853209036]; `failure_percentile` = false, `failure_bca` = true) -- F4 HOLDS against a WITH deployment and its first clause is false of the registered WITHOUT deployment; both readings stand side by side (addendum 5 item 3).
- Fits of M0 that exist are per-fold and predict only their fold's hidden corpus rows: WITH (discovery records, `evaluation/discovery/B5/`) V1 39, V2 12, V5 primary 28, V5 hno3-only 27, V5 strict 18 folds; WITHOUT (`evaluation/h3/records/B5/WITHOUT/`) V1 39, V2 12, V5 primary 28 folds (`WITH_discovery_records`, `WITHOUT_h3_records`). Example: `evaluation/h3/records/B5/WITHOUT/V5__primary_batched_max4/s104729/s104729_S_b000.json` -- `n_train` 6956, `n_actinide_training_rows_dropped` 4634, `selected_config` depth8_l23, 557 parquet rows of `row_id`, `mean_logD`, intervals and `conformal_q*` (`example_without_record`). No fitted model object is stored and no record predicts a requested (acid, ligand) condition; **no full-corpus fit of M0 exists, WITH or WITHOUT** (`what_a_record_holds`). The task's contingency "only the WITH records exist as a full-corpus fit" does not arise.
- Even once both blockers are lifted, the run would be exploratory only and would use the flagged truncated-Gaussian draw (`gen19ct/process/monte_carlo.py` `DRAW_MODE_FALLBACK`): M0 is a CatBoost arm with no M7 member means, so the request's `member_logD_k` / `conformal_q95` columns cannot be filled as section 14's "one ensemble member plus a conformal residual" requires (addendum 2 item 5 `fallback`).

## Null / supported / ambiguous

- **None applies. H7 is NOT_RUN.** S2(d) is NOT_EVALUATED (conditional on Phase H; `decisions/CONFIRMATION_PLAN.md` section 3.4, bullet "**S2(d)** is **not evaluated**"). F5(i) and F5(ii) are NOT_EVALUATED (section 10: "only if Phase H runs"). No process number exists, so nothing here is evidence for or against the process claim, and this is not a null: no INFORMATIVE_NULL power record exists for H7 and none could.
- The registered label of anything the process layer produces while S1 is UNDECIDED and V6 has not run is **transfer-unsupported**; the exploratory run that would have carried it did not start.

## Decision

- No Gen19 process recommendation exists for the TODGA Pr/Nd nitrate case. gen18's chain keeps its B1 nearest-condition lookup as the default D source (section 10, last paragraph). PC88A, Cyanex 272 and D2EHPA stay literature-only (section 14 "Case").
- Brief section 34 questions 9 ("do Gen18 process recommendations remain good after uncertainty propagation?") and 10 (which Pr/Nd recommendations are directly supported / transfer-supported / speculative / unsupported) are **not answered**.
- The deployed predictor is M0 (alias B5 in every lookup), and the fit deployed for lanthanide prediction is the WITHOUT-actinide one (section 11, addendum 5 item 3); no deployable instance of it has been produced.
- No code was changed by this task, so `manifests/digest_registry.json` -> `code_changes` receives no entry from it. The uncommitted edit to `scripts/g19_run_discovery.py` found in the working copy (`concurrent_tree_modification`) is not this task's; at the time of writing it had no `code_changes` entry, and its author had to log it (addendum 2 item 5).
  - **Update 2026-09-24:** the edit was logged as `manifests/digest_registry.json` -> `code_changes[40]` (`logged_utc` 2026-09-23T14:23:07+00:00; files `scripts/g19_run_discovery.py`, sha256_lf `2933bb8a...`, and `tests/test_registry.py`) by the figures / D07 task; blocker 1 is therefore closed (the verification-basis change: a record set is verified under the registry entry it was written under, addendum 7 item 3), and **blocker 2 -- the prediction table has no producer -- remains the operative blocker.**

## Next action

1. Orchestrator decision on blocker 1: the discovery-completeness verification (`h3.discovery_complete` via `g19_run_discovery.expected_fold_records` / `fold_digest`) must verify a record against the registry entry it was written under -- the superseded-entry fallback `registry.verify_record` already implements -- instead of the current entry's below-footer digest. An uncommitted edit doing this already sits in the working copy (`concurrent_tree_modification`, not this task's); review it, run the tests that cover the gate, log it in `manifests/digest_registry.json` -> `code_changes` with its reason (addendum 2 item 5), and commit it. It validates or invalidates no record. **Status 2026-09-24: logged as `code_changes[40]` (14:23:07Z) with its test `tests/test_registry.py::test_a_record_set_is_verified_under_the_entry_it_was_written_under`; blocker 1 is closed, the commit is the orchestrator's; blocker 2 (item 2 below) is the one that remains operative.**
2. Build the deployment prediction step: fit M0 (B5) WITHOUT actinide rows on the section 2 scored population at the H3 WITHOUT selections, with cross-fitted conformal calibration; predict the 450 rows of `evaluation/process/inputs/todga_prnd_prediction_request.csv` with section 13 labels (`domain_status`, `support_score`, `nearest_support`) from a `SupportIndex` fitted on the training rows; write `evaluation/process/inputs/todga_prnd_predictions.csv` with `arm` = B5 (the alias the runner and tables use). Record that the member columns cannot be filled for a CatBoost arm, so the draw is the flagged fallback.
3. Estimate rho from comparable-pair residuals in inner folds, no `V6_TARGET_ROWS` row (section 14), and write `evaluation/process/inputs/prnd_residual_correlation.json`.
4. Then `scripts/g19_run_process.py --exploratory` with the registered 64 draws; reduce `--n-lhs` below 1,000 only if the 2 h cap requires it, and record the reduction as exploratory. Every output stays `transfer-unsupported`.
5. A registered (headline-capable) run needs `evaluation/confirmation/decisions/confirmation.json` with `S1.passed`, `S2.passed` and `V6.run` all true, which only the confirmation run can write; that run has not started.

Files: `evaluation/process/attempts/g2_exploratory_attempt_2026-09-23.json` (present); `evaluation/process/inputs/todga_prnd_prediction_request.csv` (present); `evaluation/process/summary.json`, `winners.csv`, `stability.json`, `f5.json`, `operating_points.csv`, `process_table.csv`, `figures/F14_process_pareto_uncertainty.png`, `figures/F15_probability_of_specification_map.png` (absent -- not computed).
