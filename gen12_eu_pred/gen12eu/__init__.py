"""Gen12Eu_pred — single-lanthanide (Eu) prediction on unseen extractants.

Isolated from gen1–gen11: nothing here writes into ``runs/`` or ``src/``.  The
repository package ``lanthanide_separation`` is imported *read-only* for the
definitions that must stay identical to the frozen programme (condition ids,
series ids, bit-identical ECFP clusters, single-linkage chemotypes, the
randomised grouped K-fold and the paired block bootstrap).
"""
__version__ = "0.1.0"
